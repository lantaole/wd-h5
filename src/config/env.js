/**
 * 配置开发环境和生产环境变量
 * domain 当前环境域名
 * bucket: 阿里云存储域地址
 */

// const domain = window.location.protocol + "//" + window.location.host;
const aliTokenUrl = "oss/get-oss-token";

export { aliTokenUrl };
